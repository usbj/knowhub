<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.knowhub.mapper.tag.TagMapper">

    <resultMap id="TagResultMap" type="com.knowhub.pojo.tag.entity.Tag">
        <id property="tagId" column="tag_id"/>
        <result property="tagName" column="tag_name"/>
        <result property="description" column="description"/>
        <result property="sort" column="sort"/>
        <result property="status" column="status"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="deleted" column="deleted"/>
    </resultMap>

    <select id="quarryTag" parameterType="com.knowhub.pojo.tag.vo.TagVo" resultMap="TagResultMap">
        select * from tag
        where deleted = 0
        <if test="tagName != null and tagName != ''">and tag_name like concat('%', #{tagName}, '%')</if>
        <if test="status != null">and status = #{status}</if>
        order by sort asc, tag_id asc
    </select>

    <select id="getTagInfoById" parameterType="long" resultMap="TagResultMap">
        select * from tag where tag_id = #{tagId} and deleted = 0
    </select>

    <insert id="addTag" parameterType="com.knowhub.pojo.tag.entity.Tag" useGeneratedKeys="true" keyProperty="tagId">
        insert into tag
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="tagName != null">tag_name,</if>
            <if test="description != null">description,</if>
            <if test="sort != null">sort,</if>
            <if test="status != null">status,</if>
            create_by, create_time, update_by, update_time
        </trim>
        values
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="tagName != null">#{tagName},</if>
            <if test="description != null">#{description},</if>
            <if test="sort != null">#{sort},</if>
            <if test="status != null">#{status},</if>
            #{createBy}, now(), #{updateBy}, now()
        </trim>
    </insert>

    <update id="editTagInfo" parameterType="com.knowhub.pojo.tag.entity.Tag">
        update tag
        <set>
            <if test="tagName != null and tagName != ''">tag_name = #{tagName},</if>
            <if test="description != null">description = #{description},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="status != null">status = #{status},</if>
            <if test="deleted != null">deleted = #{deleted},</if>
            update_by = #{updateBy},
            update_time = now()
        </set>
        where tag_id = #{tagId}
    </update>

    <update id="softDeleteTag" parameterType="long">
        update tag set deleted = 1, update_time = now() where tag_id = #{tagId}
    </update>

    <!-- 校验用：取给定 id 集合中启用且未删除的标签 -->
    <select id="getEnabledTagsByIds" parameterType="list" resultMap="TagResultMap">
        select * from tag
        where deleted = 0 and status = 1
          and tag_id in
        <foreach collection="list" item="id" open="(" separator="," close=")">#{id}</foreach>
    </select>

</mapper>
