<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.knowhub.mapper.common.NoticeReadMapper">

    <!-- 查当前用户对某公告的确认态：confirm_status（1=已确认，0=已读未确认），无记录返 null -->
    <select id="getConfirmStatus" resultType="java.lang.Integer">
        select confirm_status from sys_notice_read
        where notice_id = #{noticeId} and user_id = #{userId}
        limit 1
    </select>

    <!-- 批量取当前用户已确认的 noticeId 集合（confirm_status=1），用于列表回填 hasConfirmed -->
    <select id="getConfirmedNoticeIds" resultType="java.lang.Long">
        select notice_id from sys_notice_read
        where user_id = #{userId} and confirm_status = 1
        and notice_id in
        <foreach collection="noticeIds" item="id" open="(" separator="," close=")">#{id}</foreach>
    </select>

    <!-- 插入一条读记录并直接置 confirm_status=1（"确认"时该用户尚无读记录的场景） -->
    <insert id="insertConfirmRecord">
        insert into sys_notice_read (notice_id, user_id, read_time, confirm_status, confirm_time)
        values (#{noticeId}, #{userId}, now(), 1, now())
    </insert>

    <!-- 把已有读记录的 confirm_status 置 1（"确认"时该用户已读未确认的场景） -->
    <update id="updateConfirmStatus">
        update sys_notice_read
        set confirm_status = 1, confirm_time = now()
        where notice_id = #{noticeId} and user_id = #{userId}
    </update>
</mapper>